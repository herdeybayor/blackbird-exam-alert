# =====================================================
# Exam Alert System - Environment Configuration
# =====================================================
# Copy this file to .env and update with your values

# =====================================================
# DATABASE CONFIGURATION
# =====================================================
# PostgreSQL database connection URL
# Format: postgresql://username:password@hostname:port/database_name?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/exam_alert_db?schema=public"

# Direct database URL for migrations (usually the same as DATABASE_URL)
DIRECT_URL="postgresql://user:password@localhost:5432/exam_alert_db?schema=public"

# =====================================================
# EMAIL SERVICE CONFIGURATION (SMTP)
# =====================================================
# SMTP server hostname (e.g., smtp.gmail.com, smtp.outlook.com, smtp.sendgrid.net)
SMTP_HOST=smtp.gmail.com

# SMTP server port (587 for TLS, 465 for SSL, 25 for unsecured)
SMTP_PORT=587

# Use secure connection (true for SSL/TLS, false for unsecured)
SMTP_SECURE=false

# Email authentication credentials
SMTP_USER=your-email@example.com
SMTP_PASS=your-app-password

# From email address (usually the same as SMTP_USER)
SMTP_FROM=your-email@example.com

# =====================================================
# ADMIN CONFIGURATION
# =====================================================
# Default admin password (will be hashed on first login)
ADMIN_DEFAULT_PASSWORD=admin123456

# =====================================================
# APPLICATION CONFIGURATION
# =====================================================
# Next.js environment (development, production, test)
NODE_ENV=development

# Application URL (used for links in emails)
APP_URL=http://localhost:3000

# =====================================================
# EMAIL PROVIDER SPECIFIC CONFIGURATIONS
# =====================================================

# --- GMAIL CONFIGURATION ---
# For Gmail, use app-specific passwords instead of regular passwords
# 1. Enable 2-Factor Authentication in your Google account
# 2. Go to Google Account Settings > Security > App passwords
# 3. Generate an app password for "Mail"
# 4. Use this app password in SMTP_PASS
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-gmail@gmail.com
# SMTP_PASS=your-16-character-app-password

# --- OUTLOOK/HOTMAIL CONFIGURATION ---
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@outlook.com
# SMTP_PASS=your-password

# --- SENDGRID CONFIGURATION ---
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key

# --- MAILGUN CONFIGURATION ---
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=postmaster@your-domain.mailgun.org
# SMTP_PASS=your-mailgun-smtp-password

# --- AMAZON SES CONFIGURATION ---
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-ses-smtp-username
# SMTP_PASS=your-ses-smtp-password

# =====================================================
# OPTIONAL CONFIGURATIONS
# =====================================================

# Email rate limiting (emails per minute, default: 10)
EMAIL_RATE_LIMIT=10

# Maximum email recipients per batch (default: 50)
EMAIL_BATCH_SIZE=50

# Email retry attempts for failed sends (default: 3)
EMAIL_RETRY_ATTEMPTS=3

# Enable email delivery tracking (default: false)
EMAIL_TRACKING_ENABLED=false

# Default timezone for the application (default: UTC)
TZ=UTC

# =====================================================
# SECURITY NOTES
# =====================================================
# - Never commit the .env file to version control
# - Use strong, unique passwords
# - Enable 2FA where possible
# - Regularly rotate credentials
# - Use app-specific passwords for Gmail
# - Consider using environment-specific configurations
# - Monitor email sending quotas and limits